# NET-014

## Symptom
Hostname lookup fails even though the DNS server is reachable.

## Topology
DNS server is 192.168.1.2 and web server is 192.168.1.3.

## Show Outputs
show running-config / DNS service -> A record for the hostname is missing.

## Expected Fault
Required DNS A record is missing.

## OSI Layer
Layer 7

## Concept
DNS records

## Severity
Medium
